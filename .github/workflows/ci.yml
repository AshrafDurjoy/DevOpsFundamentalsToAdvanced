name: 08 - CI Pipeline Demo
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: |
          # Simulate installing dependencies
          echo "Installing dependencies..."
          # In a real project this would be:
          # npm ci
          mkdir -p node_modules
          echo "Dependencies installed successfully!"
      - name: Build
        run: |
          # Simulate building the project
          echo "Building project..."
          mkdir -p build
          echo "<html><body><h1>Demo App</h1></body></html>" > build/index.html
          echo "Build completed successfully!"
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-files
          path: build/

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: |
          # Simulate installing dependencies
          echo "Installing dependencies..."
          mkdir -p node_modules
      - name: Run unit tests
        run: |
          # Simulate running tests
          echo "Running unit tests..."
          mkdir -p test-results
          echo "All tests passed!" > test-results/unit-test-results.txt
      - name: Run integration tests
        run: |
          # Simulate integration tests
          echo "Running integration tests..."
          echo "All integration tests passed!" > test-results/integration-test-results.txt
      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/

  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: |
          # Simulate installing dependencies
          echo "Installing dependencies..."
          mkdir -p node_modules
      - name: Lint code
        run: |
          # Simulate linting
          echo "Linting code..."
          echo "No linting errors found!"
      - name: Check formatting
        run: |
          # Simulate format check
          echo "Checking formatting..."
          echo "All files are properly formatted!"